// displays icons and description for each movie shown

export interface MoviesList {
  id: number;
  title: string;
  posterPath: string;
  overview: string;
  releaseDate: string;
  popularity: number;
}
